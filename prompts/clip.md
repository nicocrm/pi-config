---
description: Copy the last response to clipboard
---
Take your last response and copy it to the system clipboard. Use `pbcopy` on macOS or `xclip -selection clipboard` on Linux — detect the OS automatically.
